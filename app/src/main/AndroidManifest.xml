<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.animexx">

    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnimeXX">
        <activity android:name=".FullimageActivity"></activity>
        <activity android:name=".fullmetal" />
        <activity android:name=".boruto" />
        <activity android:name=".dragonball" />
        <activity android:name=".tokyoghoul" />
        <activity android:name=".deathnote" />
        <activity android:name=".naruto" />
        <activity android:name=".onepiece" />
        <activity android:name=".attackontitan" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>